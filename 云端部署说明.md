# 把机器人放到云端

目标：电脑关机以后，Discord 机器人也继续在线。

推荐方式：先用 GitHub + Render 免费 Web Service 测试。

## 你需要准备

- 一个 GitHub 账号
- 一个 Render 或 Railway 账号
- Discord Bot Token
- Discord Application ID
- Discord Server ID
- Discord Channel ID
- 可选：API-Football key

## 方案 A：Render

这个机器人已经带一个很小的 `/health` 健康检查网页，所以可以先用 Render 的免费 **Web Service** 测试。

### 第 1 步：上传代码到 GitHub

1. 打开 <https://github.com/>
2. 新建一个仓库，例如：
   `worldcup-ai-discord-bot`
3. 把这个文件夹里的代码上传上去：
   `worldcup-ai-discord-bot`

需要上传的文件包括：

```text
package.json
render.yaml
src/index.js
scripts/register-commands.js
.env.example
README_中文说明.md
云端部署说明.md
```

不要上传 `.env`，因为里面有机器人 Token。

### 第 2 步：在 Render 创建 Worker

1. 打开 <https://dashboard.render.com/>
2. 点 **New**
3. 选择 **Web Service**
4. 连接你的 GitHub 仓库
5. Build Command 填：

```bash
npm install
```

6. Start Command 填：

```bash
npm start
```

### 第 3 步：添加环境变量

在 Render 项目里找 **Environment**，添加这些：

```text
DISCORD_TOKEN=你的机器人Token
DISCORD_CLIENT_ID=你的Application ID
DISCORD_GUILD_ID=你的服务器ID
DISCORD_CHANNEL_ID=你的新闻频道ID
UPDATE_INTERVAL_MINUTES=60
TIMEZONE=Asia/Kuala_Lumpur
FOOTBALL_PROVIDER=demo
API_FOOTBALL_KEY=
API_FOOTBALL_LEAGUE_ID=1
API_FOOTBALL_SEASON=2026
OPENAI_API_KEY=
```

如果你有 API-Football key，再改成：

```text
FOOTBALL_PROVIDER=api-football
API_FOOTBALL_KEY=你的APIKey
```

### 第 4 步：部署

点 **Deploy**。

成功以后，Logs 里应该看到：

```text
World Cup AI Bot logged in as ...
```

### 第 5 步：注册斜杠命令

在本地电脑先运行一次：

```bash
node scripts/register-commands.js
```

如果你不想在本地运行，也可以临时把 Render 的 Start Command 改成：

```bash
node scripts/register-commands.js
```

等它成功后，再改回：

```bash
npm start
```

## 方案 B：Railway

Railway 也可以运行这个机器人。

1. 打开 <https://railway.com/>
2. 连接 GitHub 仓库
3. 新建 Project
4. 选择你的机器人仓库
5. 添加和上面一样的环境变量
6. Start Command 用：

```bash
npm start
```

## 重要提醒

### Token 不要公开

`.env` 文件不能上传到 GitHub。

如果 Token 不小心公开了：

1. 立刻去 Discord Developer Portal
2. Bot 页面
3. 点 **Reset Token**
4. 更新云端环境变量

### 免费云端可能会有限制

不同平台的免费额度会变化。有些平台免费但会休眠，有些需要绑银行卡，有些 worker 需要付费。先用免费能跑就跑；群开始有人以后，再升级稳定方案。

### 机器人不需要网页端口

这个版本会自动监听云平台提供的 `PORT`，并开放 `/health` 健康检查地址。

## 免费版说明

Render 的免费 Web Service 适合测试，不适合高要求的正式服务。Render 可能会重启免费实例，也会在长时间没有网络活动时休眠。这个机器人会持续连接 Discord，因此通常会保持活动；如果以后群里人多了，建议升级稳定方案。
